import * as _nuxt_schema from '@nuxt/schema';
import { InitOptions } from 'eruda';

type AvailableErudaPlugins = 'eruda-vue' | 'eruda-monitor' | 'eruda-features' | 'eruda-timing' | 'eruda-code' | 'eruda-benchmark' | 'eruda-geolocation' | 'eruda-orientation' | 'eruda-touches';
interface ModuleOptions {
    /**
     * 是否开启 eruda
     */
    enabled?: boolean;
    /**
     * eruda 初始化配置
     */
    initOptions?: InitOptions;
    /**
     * 添加 eruda 插件
     */
    plugins?: AvailableErudaPlugins[];
}
declare const _default: _nuxt_schema.NuxtModule<ModuleOptions, ModuleOptions, false>;

export { _default as default };
export type { AvailableErudaPlugins, ModuleOptions };
